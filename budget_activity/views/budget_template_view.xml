<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="budget_template_view_form" model="ir.ui.view">
        <field name="name">budget.template.view.form</field>
        <field name="model">budget.template</field>
        <field name="inherit_id" ref="budget_control.budget_template_view_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//field[@name='line_ids']/tree/field[@name='account_ids']"
                position="before"
            >
                <field name="activity_ids" widget="many2many_tags" />
            </xpath>
            <xpath
                expr="//field[@name='line_ids']/tree/field[@name='account_ids']"
                position="attributes"
            >
                <attribute name="readonly">1</attribute>
                <attribute name="force_save">1</attribute>
            </xpath>
            <xpath expr="//page[@name='help']//ol" position="inside">
                <li><b
                    >Activity</b>: Replaces binding with an account, where 1 activity = 1 account and each activity is associated with a KPI.</li>
            </xpath>
            <xpath expr="//page[@name='help']" position="inside">
                Hierarchy:
                <pre>
                            KPI 1                   KPI 2
                            /  \                   /   \
                          /      \               /       \
                        /          \           /           \
                    Activity1   Activity2   Activity3   Activity4
                        |            \      /               |
                        |              \  /                 |
                    Account1          Account2          Account3
                </pre>
                So, <b
                >Activity must not be repeated in order</b> for users to be able to select them,
                and the system will correctly check the budget.
            </xpath>

        </field>
    </record>
</odoo>
